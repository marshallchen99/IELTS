# CLAUDE.md

**规则全文在 [`PROJECT_INSTRUCTIONS.md`](PROJECT_INSTRUCTIONS.md)，读那一份。**

本项目的主力形态是 claude.ai Project：`PROJECT_INSTRUCTIONS.md` 的内容被粘贴在 Project 的自定义指令里，是那套规则的唯一出处。
这个文件只是一个指针，避免同一套规则存在两份、各自漂移。

如果你是在 Claude Code 里读到这里：先完整读 `PROJECT_INSTRUCTIONS.md`，再读 `IELTS_Error_Log.md`，然后按前者的「每次对话开场」执行。
